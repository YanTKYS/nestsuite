# NestSuite リリース前確認チェックリスト（v2.22.0）

> **実装時は `docs/development/nestsuite-development-guidelines.md` を参照してください。**  
> 変更履歴・チェック追加の経緯は [`nestsuite-release-checklist-history.md`](nestsuite-release-checklist-history.md) を参照してください。

---

## 0. リリース毎確認（毎回確認する）

リリースのたびに必ず確認する項目。ここが通っていなければ先に進めない。

- [ ] `NestSuite.csproj` の `InformationalVersion` が対象バージョンに更新されている
- [ ] `ApplicationVersionTests.cs` の期待値が対象バージョンと一致している（`ApplicationVersion_UsesAssemblyInformationalVersion` / `WindowTitle_UsesApplicationVersion` / `ApplicationAndSchemaVersionsAreManagedBySeparateSources`）
- [ ] `docs/release-notes.md` に対象バージョンのエントリが追加されている
- [ ] release-notes エントリに保存形式変更有無が明記されている
- [ ] release-notes エントリに session 形式変更有無が明記されている
- [ ] release-notes エントリに schema bump 有無が明記されている
- [ ] NoteNest schema が `1.4.2` のまま維持されている（`ApplicationVersionTests.NoteNestSchemaVersion_IsPinned`）
- [ ] backlog 完了項目が backlog.md から削除されている（完了済み項目は release-notes で追跡）
- [ ] `dotnet build` が通る
- [ ] `dotnet test` が通る（全テスト）
- [ ] GitHub Actions CI green
- [ ] UI Smoke Test green

---

## 1. ビルド・テスト

- [ ] `dotnet build` が通る
- [ ] `dotnet test` が通る（全テスト）
- [ ] `ApplicationVersionTests` でバージョンが正しく表示されることを確認
- [ ] `NoteNest保存スキーマが 1.4.2` のままであることを確認

---

## 2. 起動確認（NestSuite 主対象）

- [ ] `NestSuite.exe` → NestSuite として起動し、Temp タブがアクティブになる
- [ ] `NestSuite.exe sample.notenest` → NestSuite で `.notenest` タブを開く
- [ ] `NestSuite.exe sample.chatnest` → NestSuite で `.chatnest` タブを開く
- [ ] `NestSuite.exe sample.ideanest` → NestSuite で `.ideanest` タブを開く
- [ ] `NestSuite.exe --nestsuite` → NestSuite として起動する（互換フラグ、フラグなしと同じ動作）
- [ ] `NestSuite.exe --nestsuite sample.notenest` → NestSuite で `.notenest` タブを開く

### `--classic-notenest` 廃止確認

- [ ] `NestSuite.exe --classic-notenest` → classic NoteNest が起動**しない**（NestSuite が通常起動する）
- [ ] `NestSuite.exe --classic-notenest sample.notenest` → クラッシュせず、NestSuite で対象ファイルが開く

---

## 3. NestSuite 基本操作

### タブ切り替えショートカット

- [ ] ChatNest タブ上で `Shift + ← / →` により Shell のタブ切り替えが動作する
- [ ] ChatNest 入力欄フォーカス中でも `Shift + ← / →` が発言者切り替えとして処理されない
- [ ] ChatNest の発言者切り替えは `Ctrl + ← / →` で動作する
- [ ] ChatNest の投稿は `Ctrl + Enter` で動作する
- [ ] ChatNest 入力欄で `Shift + Enter` が投稿にならない
- [ ] Shell の `Ctrl + Tab` / `Ctrl + 数字` タブ切り替えが従来どおり動作する

### タブ作成

- [ ] NoteNest タブを新規作成できる
- [ ] ChatNest タブを新規作成できる
- [ ] IdeaNest タブを新規作成できる
- [ ] NoteNest タブを複数同時に開ける
- [ ] ChatNest タブを複数同時に開ける
- [ ] IdeaNest タブを複数同時に開ける

### ファイルを開く

- [ ] `.notenest` ファイルを開ける
- [ ] `.chatnest` ファイルを開ける
- [ ] `.ideanest` ファイルを開ける
- [ ] 同じ `.notenest` を再度開くと既存タブがアクティブになる
- [ ] 同じ `.chatnest` を再度開くと既存タブがアクティブになる
- [ ] 同じ `.ideanest` を再度開くと既存タブがアクティブになる

### 複数ファイル一括オープン

- [ ] 「開く...」ダイアログで複数ファイルを同時選択できる
- [ ] `.notenest` / `.chatnest` / `.ideanest` を混在して複数選択し、それぞれタブとして開ける
- [ ] 既に開いているファイルを含めても重複タブにならない（既存タブがアクティブになる）
- [ ] 一部のファイルが存在しない場合、他のファイルは開き、失敗件数・ファイル名・理由が表示される（v2.16.15）
- [ ] 失敗に破損（InvalidFormat）が含まれる場合、末尾に「単体で開き直すと詳しい .bak 復元案内が表示される」旨が追加される（v2.16.19）
- [ ] 開いたファイルが最近使ったファイルに登録される

### 最近使ったファイル

- [ ] ファイルメニューに「最近使ったファイル」サブメニューが表示される
- [ ] ファイルを開くと最近使ったファイルの先頭に追加される（3 ツール横断）
- [ ] 同じファイルを再度開くと重複せず先頭に移動する
- [ ] 最近使ったファイルから既存タブのファイルを選ぶと既存タブがアクティブになり、履歴でも先頭へ移動する
- [ ] 「開く...」から既存タブのファイルを選んだ場合も、履歴の先頭へ移動する
- [ ] 最近使ったファイルから削除・移動済みのファイルを選ぶとエラーダイアログが表示され履歴から削除される
- [ ] 最近使ったファイルに未対応拡張子のパスが混入している場合、エラーダイアログが表示され履歴から削除される
- [ ] NoteNest タブで「名前を付けて保存」すると保存先が最近使ったファイルに追加される
- [ ] アプリ再起動後も最近使ったファイルが引き継がれる

### 保存

- [ ] NoteNest タブを上書き保存できる（メニュー・Ctrl+S 両方）
- [ ] ChatNest タブを上書き保存できる（メニュー・Ctrl+S 両方）
- [ ] IdeaNest タブを上書き保存できる（メニュー・Ctrl+S 両方）
- [ ] 保存後に未保存マーク（`*`）が消える
- [ ] 名前を付けて保存で別タブが開くパスを指定するとエラーが表示される

### 自動保存（v2.14.12 SH-33）

- [ ] 既存の保存先を持つ NoteNest/IdeaNest/ChatNest タブを編集後、手動保存せず放置すると自動保存されること（未保存マークが消える）
- [ ] 新規未保存タブは自動保存されないこと
- [ ] TempNest の自動保存間隔・動作に影響がないこと

### Ctrl+S ショートカット

- [ ] NoteNest タブで Ctrl+S を押すと上書き保存される
- [ ] ChatNest タブで Ctrl+S を押すと上書き保存される
- [ ] IdeaNest タブで Ctrl+S を押すと上書き保存される
- [ ] 未保存（パス未設定）タブで Ctrl+S を押すと「名前を付けて保存」に進む
- [ ] メニューの「上書き保存」に Ctrl+S が自動表示される
- [ ] メニューの「上書き保存」クリックと Ctrl+S の挙動が一致する

### タブを閉じる

- [ ] 保存済みタブを閉じられる
- [ ] 未保存タブを閉じようとすると確認ダイアログが表示される（タブ名が明示されている）
- [ ] 確認でキャンセルするとタブが残る
- [ ] 確認で破棄するとタブが閉じる
- [ ] 最後の通常タブを閉じると Temp タブがアクティブになる

### タブ復元

- [ ] 保存済みファイルタブを開いた状態でアプリを終了し、再起動すると前回のタブが復元される
- [ ] `.notenest` / `.chatnest` / `.ideanest` の 3 形式すべてが復元される
- [ ] 前回アクティブだったタブが復元後に選択されている
- [ ] 未保存タブ（無題）は復元されない
- [ ] `NestSuite.exe sample.notenest` など引数指定起動でも前回タブが復元され、指定ファイルが追加タブとして開く
- [ ] 引数指定ファイルが復元済みタブと同じ場合は重複タブを作らず既存タブがアクティブになる
- [ ] セッションに記録されたファイルが存在しない場合はスキップして他のファイルを復元する
- [ ] 復元対象が 1 件もない場合は Temp タブがアクティブになる
- [ ] 読めない `.nestsuite`（内容が破損・種別判定不能等）がセッションに含まれる場合、まとめて 1 回通知され、他の正常なタブは復元される
- [ ] FileNotFound を含む session 復元失敗時、復元失敗一覧と「次回から再試行しない」確認が 1 つのダイアログで表示される（v2.16.21 SH-34。ダイアログが 2 枚連続で出ることはない）
- [ ] 「はい」を選ぶと、見つからないファイルは次回以降の復元対象から外れる。「いいえ」を選ぶと、従来どおり次回も再試行される
- [ ] FileNotFound を含まず InvalidFormat のみの場合は、OK 通知のみで「単体で開き直すと詳しい .bak 復元案内が表示される」旨が末尾に出る（v2.16.19）
- [ ] FileNotFound と InvalidFormat が混在する場合、1 つのダイアログ内に .bak 誘導と FileNotFound 解除確認の両方が表示される

---

## 4. 独立性確認

- [ ] 複数の NoteNest タブで内容が混ざらない
- [ ] NoteNest と ChatNest と IdeaNest のタブで内容が混ざらない
- [ ] タブ A を保存してもタブ B の未保存状態に影響しない
- [ ] タブ A を編集してもタブ B の内容に影響しない

---

## 4.5. ツールメニュー確認

- [ ] ツールメニューから NoteNest タブへ移動または新規作成できる
- [ ] ツールメニューから IdeaNest タブへ移動または新規作成できる
- [ ] ツールメニューから ChatNest タブへ移動または新規作成できる
- [ ] アクティブなタブに対応するツールメニュー項目にチェックマークが付く

---

## 4.6. IdeaNest 編集導線確認

### 詳細画面の基本操作

- [ ] カードをクリックすると詳細画面が開き、タイトル・本文・タグが即座に編集可能な状態で表示される
- [ ] 「✎ 編集」ボタンが表示されない
- [ ] タイトル TextBox に直接入力できる
- [ ] 本文 TextBox に直接入力でき、Enter で改行できる
- [ ] タグ TextBox にカンマ区切りでタグを入力できる
- [ ] カラーピッカー（色丸ボタン）を選択すると背景色が即座に変わる
- [ ] 「📌 ピン留め」ボタンをクリックすると ★ アイコンが表示され、ボタンラベルが「ピン留め解除」に変わる
- [ ] 「📥 アーカイブ」ボタンをクリックするとアーカイブ済みバッジが表示され、ボタンラベルが「アーカイブ解除」に変わる

### 保存タイミング

- [ ] 詳細画面を閉じると編集内容がカードに反映される（カード一覧のタイトル・色等が更新される）
- [ ] 「前へ」ボタンで前のカードへ移動すると、移動前のカードの編集内容が保存される
- [ ] 「次へ」ボタンで次のカードへ移動すると、移動前のカードの編集内容が保存される
- [ ] Ctrl+S を押すと即座に保存される（ウィンドウは閉じない）
- [ ] 何も変更せず閉じた場合、UpdatedAt が更新されない

### キーボード操作

- [ ] Escape キーで詳細画面を閉じる（編集内容は保存される）
- [ ] 本文 TextBox にフォーカスがある状態で ← / → キーを押してもカード移動しない（テキストカーソルが動く）
- [ ] 本文 TextBox にフォーカスがない状態で ← / → キーを押すとカードを移動する

### 旧編集ダイアログへの導線廃止確認

- [ ] カードにカーソルを合わせても「✎ 編集」ボタンが表示されない
- [ ] カードの右クリックコンテキストメニューに「編集」項目が表示されない
- [ ] 「＋」ボタンを押すと、旧編集ダイアログではなく詳細画面ベースの新規作成画面が開く
- [ ] 新規作成画面のタイトルが「新規アイデア」になっている
- [ ] 新規作成画面で前へ/次へボタンが無効になっている

### 新規作成確認

- [ ] 新規作成画面で日付行（作成日・更新日）が表示されない
- [ ] 新規作成画面でタイトル・本文・タグ・色・ピン留め・アーカイブを入力できる
- [ ] 何も入力せず閉じた場合、カードが追加されない
- [ ] 色を選んだだけ・ピン留めを押しただけで閉じた場合、カードが追加されない
- [ ] タイトルだけ入力して閉じると、カードが追加される
- [ ] 本文だけ入力して閉じると、カードが追加される
- [ ] タグだけ入力して閉じると、カードが追加される
- [ ] Ctrl+S を押すとカードが追加される
- [ ] Ctrl+S 後に閉じても同じカードが二重作成されない
- [ ] Ctrl+S 後に追加編集してから閉じると、追加編集分も保存される
- [ ] 再起動後も新規作成したカードが残っている

---

## 5. タブ表示確認

- [ ] タブ見出しに絵文字プレフィックスと拡張子なしファイル名が表示される（例：`📝 業務改善` / `💬 開発メモ` / `💡 ツール改修`）
- [ ] Temp タブの見出しは「Temp」のみ（絵文字プレフィックスなし）で表示される
- [ ] 未保存タブに `●` が表示される
- [ ] 保存後に `●` が消える
- [ ] タブのツールチップが「種類 / ファイル名 / 場所 / 状態」の 4 行形式で表示される
- [ ] 無題タブのツールチップに「ファイル名: 未保存（無題）」「場所: —」「状態: 未保存」が表示される
- [ ] Temp タブのツールチップに「種類: TempNest」「説明: 一時メモ」「保存: 自動保存」が表示される

---

## 6. About / バージョン表示確認

- [ ] About に「試験統合版」という古い文言が含まれていない
- [ ] About に「統合検証中」という古い文言が含まれていない
- [ ] バージョン番号が最新版になっている

---

## 7. ドキュメント確認

- [ ] README に NestSuite の起動方法が記載されている
- [ ] `docs/guide/nestsuite-user-guide.md` が存在する
- [ ] `docs/design/nestsuite-known-limitations.md` が存在する
- [ ] `docs/release-notes.md` に最新バージョンエントリが記載されている
- [ ] NoteNest の facade プロパティ・`NotePropertyChanged` allow-list・`BuildModels()` を変更した場合、`docs/development/coordinator-notification-pattern.md` のチェックリストを確認済み

---

## 8. シングルインスタンス・関連付け起動確認

- [ ] NestSuite 起動済みの状態で `.notenest` をダブルクリックすると既存ウィンドウに NoteNest タブが追加される
- [ ] NestSuite 起動済みの状態で `.chatnest` をダブルクリックすると既存ウィンドウに ChatNest タブが追加される
- [ ] NestSuite 起動済みの状態で `.ideanest` をダブルクリックすると既存ウィンドウに IdeaNest タブが追加される
- [ ] NestSuite 起動済みの状態で `.nestsuite` をダブルクリックすると既存ウィンドウにファイル内容に応じた Workspace タブが追加される
- [ ] 既に開いているファイルを再度開くと既存タブがアクティブになる（重複タブなし）
- [ ] 既存ウィンドウが最小化されている場合、ファイルを受け取ると前面表示される
- [ ] ファイル指定なしで 2 プロセス目を起動した場合、既存ウィンドウに変化がない
- [ ] `--classic-notenest` を指定しても NestSuite のシングルインスタンス制御が通常どおり動作する
- [ ] 読めない `.nestsuite`（内容が破損・種別判定不能等）をダブルクリックすると理由が通知される（無言で終わらない）

### 未起動時の関連付け起動＋タブ復元

- [ ] NestSuite 未起動状態で `.notenest` をダブルクリックすると、前回タブが復元されたうえで対象ファイルが追加タブとして開く
- [ ] NestSuite 未起動状態で `.chatnest` をダブルクリックすると、前回タブが復元されたうえで対象ファイルが追加タブとして開く
- [ ] NestSuite 未起動状態で `.ideanest` をダブルクリックすると、前回タブが復元されたうえで対象ファイルが追加タブとして開く
- [ ] NestSuite 未起動状態で `.nestsuite` をダブルクリックすると、前回タブが復元されたうえでファイル内容に応じた Workspace タブが追加タブとして開く
- [ ] 追加で開いたファイルのタブがアクティブになる
- [ ] 引数指定ファイルが復元済みタブと同じ場合は重複タブを作らず復元済みタブがアクティブになる

---

## 9. NoteNest Workspace エディタ確認

### 行番号表示

- [ ] NoteNest Workspace を開くと、本文エディタ左側に行番号が表示される
- [ ] 新規（無題）NoteNest タブを作成しても行番号が表示される
- [ ] 既存 `.notenest` ファイルを開いても行番号が表示される
- [ ] 本文をスクロールしても行番号とずれない（スクロール同期が維持される）
- [ ] 行番号表示の ON/OFF UI（メニュー・チェックボックス等）が追加されていない

### 現在行強調

- [ ] 行番号ガター上でカーソルがある行が背景ハイライトされる
- [ ] ライトテーマ・ダークテーマ両方で現在行強調が確認できる

### 簡易ノートリンク補完

- [ ] `[[` を入力するとノートタイトルの候補 Popup が表示される
- [ ] ↑↓ キーで候補を移動できる
- [ ] Tab キーで `[[ノート名]]` が挿入される
- [ ] Esc キーでキャンセルできる
- [ ] タスクコメント編集中は補完が抑制される

---

## 10. TempNest 固定タブ確認

### 基本表示

- [ ] タブストリップの左端に「Temp」タブが常に表示される
- [ ] 「Temp」タブに × ボタンが表示されない
- [ ] 「Temp」タブを右クリックすると「このタブを閉じる」が無効（グレーアウト）になっている
- [ ] 「Temp」タブのカラーアクセントがグレー系（他の Workspace と異なる色）で表示される
- [ ] 「Temp」タブを中クリックしても閉じない
- [ ] 「Temp」タブをドラッグしても移動できない

### TempNest Workspace 表示

- [ ] 「Temp」タブを選択すると 2×2 の 4 スロットが表示される
- [ ] 各スロットにタイトル欄（1 行）・本文欄（複数行）・「コピー」ボタン・「クリア」ボタンがある
- [ ] ステータスバーの右端に「/ TempNest」が表示される
- [ ] TempNest 選択中はツールメニューの NoteNest / IdeaNest / ChatNest のチェックマークがすべて外れる

### メモ操作

- [ ] タイトル欄に入力できる
- [ ] 本文欄に入力でき、Enter で改行できる
- [ ] 「コピー」ボタンをクリックすると本文がクリップボードにコピーされる
- [ ] 本文が空の場合「コピー」ボタンが無効になる
- [ ] 「クリア」ボタンをクリックするとタイトルと本文が空になる

### 自動保存・起動時復元

- [ ] 入力後 1 秒程度待つと自動保存される（アプリを再起動すると内容が保持されている）
- [ ] アプリ終了時にも保存される（終了直前に入力した内容が再起動後に残っている）
- [ ] 保存ファイルは `%APPDATA%\NoteNest\tempnest.json` に作成される

### 通常操作との独立性

- [ ] TempNest タブ選択中に Ctrl+S を押しても保存ダイアログが開かない（無視される）
- [ ] TempNest は「最近使ったファイル」に登録されない
- [ ] TempNest はタブセッション復元の対象にならない（セッションファイルにパスが書き出されない）
- [ ] 「他のタブを閉じる」コンテキストメニューを選択しても Temp タブは閉じない（他の通常タブだけ閉じる）
- [ ] TempNest への変更があっても未保存確認ダイアログが表示されない（終了時・タブクローズ時）

### TempNest 初期改善

- [ ] セッション復元あり起動で、Temp タブが一瞬表示されてから前回タブに切り替わるちらつきが発生しない
- [ ] セッション復元なし起動で、Temp タブが最初からアクティブになる
- [ ] 各スロットのタイトル欄が空のとき「タイトル」とプレースホルダーが薄い文字で表示される
- [ ] 各スロットの本文欄が空のとき「一時メモ」とプレースホルダーが薄い文字で表示される
- [ ] プレースホルダーはライト・ダーク両テーマで適切な `MutedFg` 色で表示される

### TempNest スロット操作

- [ ] TempNest スロットの「コピー」ボタンを押すと「コピーしました」テキストがボタン行に表示される
- [ ] 「コピーしました」テキストは約 1.5 秒後に自動的に消える
- [ ] 本文が空のスロットでは「コピー」ボタンがグレーアウト（無効）になっている
- [ ] タイトルと本文がともに空のスロットでは「クリア」ボタンがグレーアウト（無効）になっている

---

## 11. NoteNest 追加機能確認

### 空状態ガイドテキスト

- [ ] 新規 NoteNest タブを作成し、ノートが0件の状態で左ペインに「＋ で最初のノートを作成」が薄い文字で中央表示される
- [ ] ノートを1件追加するとガイドテキストが消える
- [ ] ノートを全て削除するとガイドテキストが再表示される

### ノート複製（L11）

- [ ] ノート一覧でノートを右クリックすると「複製」メニューが表示される
- [ ] 「複製」をクリックすると同じノートブック内に新ノートが追加される
- [ ] 複製後のタイトルが「元タイトル のコピー」形式になっている
- [ ] 本文（マーカー・タスクを含む）が複製される
- [ ] 複製元ノートのタイトル・本文に変化がない

### リンク切れ手動チェック（M11）

- [ ] 左ペインの「＋」ボタンから「リンク切れを確認...」を選択するとダイアログが開く
- [ ] リンク切れが存在する場合、ソースノート名・リンク名・行番号・行の内容が一覧表示される
- [ ] リンク切れが 0 件の場合、「リンク切れはありません ✓」メッセージが表示される
- [ ] 「このノートへ移動」をクリックすると対象ソースノートへ移動しダイアログが閉じる
- [ ] 存在しないノートタイトルへの `[[リンク名]]` がリンク切れとして検出される

### ノート名変更リンク影響警告（M9）

- [ ] 他ノートに `[[旧ノート名]]` が存在する状態でリネームすると「リンク影響の警告」ダイアログが表示される
- [ ] 「はい」（続行）を選択するとリネームが実行される（自動修復はされない）
- [ ] 「いいえ」（キャンセル）を選択するとリネームされない
- [ ] 他ノートに参照が存在しない場合は警告なしで即時リネームされる

---

## 12. IdeaNest 追加機能確認

### 空状態テキスト（ID-11）

- [ ] IdeaNest でカードが0件のとき「まだアイデアがありません」とサブテキストが中央表示される
- [ ] 検索・フィルタ結果が0件のとき「条件に一致するカードがありません」とサブテキストが中央表示される

---

## 13. タブ UI 確認

### タブ見出し整形（SH-14）

- [ ] NoteNest タブの見出しが「📝 ファイル名（拡張子なし）」形式で表示される
- [ ] ChatNest タブの見出しが「💬 ファイル名（拡張子なし）」形式で表示される
- [ ] IdeaNest タブの見出しが「💡 ファイル名（拡張子なし）」形式で表示される
- [ ] Temp タブの見出しが「Temp」のみで表示される
- [ ] 無題タブの見出しが「📝 無題」形式で表示される

### タブドラッグ挿入位置インジケーター（SH-17）

- [ ] 通常タブをドラッグすると、挿入予定位置に細い縦線インジケーター（青色）が表示される
- [ ] マウス位置に応じてインジケーターがリアルタイムに移動する
- [ ] ドロップ後にインジケーターが消える
- [ ] Temp タブ自体はドラッグできない（左端固定のまま）
- [ ] 通常タブを Temp タブより左に移動できない

---

## 14. 保存・終了確認

### NoteNest 未保存終了確認

- [ ] NoteNest タブが未保存の状態でタブ × をクリックすると「保存/保存しない/キャンセル」の 3 択ダイアログが表示される
- [ ] 「保存」を選ぶと保存してからタブが閉じる
- [ ] 「保存」で保存先未設定の場合は「名前を付けて保存」ダイアログが表示される
- [ ] 名前を付けて保存でキャンセルするとタブが閉じない（未保存状態を維持する）
- [ ] 「保存しない」を選ぶと保存せずタブが閉じる
- [ ] 「キャンセル」を選ぶとタブが閉じない
- [ ] アプリ終了時に未保存 NoteNest があると同様の 3 択ダイアログが表示される

### 保存処理

- [ ] NoteNest を新規保存できる（`.notenest` ファイルが作成される）
- [ ] NoteNest を上書き保存できる（`.bak` が生成される）
- [ ] ChatNest を新規保存できる（`.chatnest` ファイルが作成される）
- [ ] ChatNest を上書き保存できる
- [ ] IdeaNest を新規保存できる（`.ideanest` ファイルが作成される）
- [ ] IdeaNest を上書き保存できる（`.bak` が事前コピーで生成される）
- [ ] 新規保存後に `.tmp` ファイルが残らない（3 ツール共通）
- [ ] 上書き保存後に `.tmp` ファイルが残らない（3 ツール共通）
- [ ] 存在しない保存先ディレクトリへ保存しても成功する（3 ツール共通）

### Detached 中の保存確認

- [ ] 別ウィンドウ表示中の NoteNest タブを Shell 側で閉じると保存確認が表示される
- [ ] 保存確認でキャンセルするとタブもウィンドウも残る
- [ ] 保存確認で保存成功するとタブとウィンドウが閉じる
- [ ] 別ウィンドウの × ボタンをクリックすると保存確認なしで Shell タブへ戻る

---

## 15. 別ウィンドウ表示の基本動作

### 分離・再統合

- [ ] NoteNest タブのコンテキストメニューに「別ウィンドウで表示(_D)」が表示される
- [ ] IdeaNest タブのコンテキストメニューに「別ウィンドウで表示(_D)」が表示される
- [ ] ChatNest タブのコンテキストメニューに「別ウィンドウで表示(_D)」が表示される
- [ ] TempNest タブのコンテキストメニューに「別ウィンドウで表示(_D)」が表示されない
- [ ] 「別ウィンドウで表示」を選択すると `DetachedWorkspaceWindow` が開く
- [ ] 別ウィンドウ内でコンテンツが正常に表示される（NoteNest / IdeaNest / ChatNest）
- [ ] 別ウィンドウの × を押すと保存確認なしで Shell タブへ戻る
- [ ] 「このタブへ戻す(_R)」でも再統合できる
- [ ] Shell 最小化時に別ウィンドウも最小化される
- [ ] Shell 復元時に別ウィンドウも復元される

### セッション非保存の確認

- [ ] 別ウィンドウ表示中にアプリを終了し、再起動すると別ウィンドウが開かない（Shell タブとして復元）
- [ ] 再起動後のタブ内容は正常に復元される

### MinWidth の確認

- [ ] `DetachedWorkspaceWindow` を横に縮めると 870px 以下にならない

---

## テキスト（`.txt`）Workspace の確認（v2.19.0 SH-43）

### 基本フロー

- [ ] ファイル → 新規 → 新規 テキスト で「無題.txt」タブを作成できる
- [ ] UTF-8（BOM なし）の `.txt` を開ける
- [ ] 日本語を編集して `Ctrl+S` で上書き保存できる
- [ ] 再度開いて内容が一致する
- [ ] 「名前を付けて保存」で `.txt` として保存できる（既定拡張子 `.txt`）
- [ ] 未保存のテキストタブを閉じようとすると保存 / 破棄 / キャンセルの3択で確認される
- [ ] 未保存のテキストタブがある状態でアプリを終了しようとすると確認される
- [ ] 保存済み `.txt` タブが次回起動時に session から復元される
- [ ] 最近使ったファイルの一覧から `.txt` を開ける
- [ ] テキストタブを別ウィンドウ化して編集・保存できる
- [ ] 既存のテキストタブが開いている状態で同じファイルを再度開くと、再読込せず既存タブがアクティブになる

### 文字コード（実機で可能な範囲）

- [ ] BOM つき UTF-8 の `.txt` を開いて同じ BOM つきで保存できる
- [ ] UTF-16（LE/BE、実ファイルがあれば）を開いて同じ形式で保存できる
- [ ] 対応外の文字コード（例: Shift_JIS で保存された `.txt`）を開こうとすると、元ファイルを変更せず安全にエラーになる

### 表示・回帰

- [ ] ライトテーマで本文が読める
- [ ] ダークテーマで本文が読める
- [ ] `.txt` の Windows ファイル関連付けダイアログに項目が追加されていない
- [ ] Shell 横断検索（`Ctrl+Shift+F`）の対象・候補列挙が `.txt` 追加後も従来どおり動作する
- [ ] NoteNest / IdeaNest / ChatNest / TempNest / `.nestsuite` の既存フローに回帰がない

---

## IdeaNest：Enterでフォーカス中カードをプレビュー（v2.19.1 ID-4）

### 基本フロー

- [ ] IdeaNest タブを開く
- [ ] `Tab` キーでカードへフォーカスを移動できる
- [ ] フォーカス中のカードにフォーカスリングが表示される
- [ ] フォーカス中のカードで `Enter` を押すとそのカードのプレビューが開く
- [ ] プレビューを `Escape` で閉じると、フォーカスが元のカード（またはカード一覧）へ戻る

### 競合回避の確認

- [ ] カードのピン留めボタンへ `Tab` でフォーカスし `Enter` を押すと、ピン留めボタンの操作が行われ、親カードのプレビューは開かない
- [ ] 検索ボックスへフォーカスし `Enter` を押しても、カードのプレビューは開かない

### 表示・状態別の確認

- [ ] ライトテーマで Enter プレビューが正常に動作する
- [ ] ダークテーマで Enter プレビューが正常に動作する
- [ ] カードサイズ S / M / L のいずれでも Enter プレビューが正常に動作する
- [ ] アーカイブ表示中でも Enter プレビューが正常に動作する
- [ ] 検索・フィルタ中でも、フォーカス中カードの Enter プレビューが正常に動作する

### 回帰確認

- [ ] カード追加・編集・ピン留め・アーカイブ・削除・Undo が従来どおり動作する
- [ ] 検索・フィルタ・ソート・カードサイズ変更が従来どおり動作する
- [ ] Markdown エクスポートが従来どおり動作する
- [ ] `PreviewIdeaWindow` 内の左右キーによるカード切り替えが従来どおり動作する
- [ ] `PreviewIdeaWindow` の `Escape` による終了が従来どおり動作する
- [ ] 矢印キーによるカード間移動・Space キーによるピン留め切替・Delete キーによる削除は、今回のバージョンでは追加されていない（誤操作扱いにならないことを確認）

---

## recent files破損時のquarantine・ErrorLog・空状態継続（v2.19.2 TD-87）

### 正常履歴

- [ ] NestSuiteで複数ファイル（NoteNest/IdeaNest/ChatNest）を開く
- [ ] アプリを終了し、再起動する
- [ ] 「最近使ったファイル」に開いていたファイルが表示される

### 破損履歴

- [ ] `%APPDATA%\NoteNest\nestsuite-recent-files.json` を手作業で破損させる（例: `{ invalid json`）
- [ ] NestSuiteが起動する（例外ダイアログが出ない）
- [ ] 「最近使ったファイル」が空になる
- [ ] 破損ファイルが `nestsuite-recent-files.json.corrupt`（または日時付き名）へ退避されている
- [ ] `%APPDATA%\NoteNest\logs\nestsuite-error.log` にErrorとして記録されている

### 再生成

- [ ] 破損後に任意のWorkspaceファイルを開く
- [ ] NestSuiteを終了・再起動する
- [ ] 「最近使ったファイル」が正常に復元される
- [ ] quarantineした `.corrupt` ファイルは残っている（上書きされていない）

### 既存退避ファイルあり

- [ ] `.corrupt` を事前に置いた状態で再度破損させても、既存の `.corrupt` を上書きせず日時付き名で別途退避される

---

## NoteNestエディタのワードラップ切替（v2.19.3 L4）

### 基本切替

- [ ] NoteNestを開く
- [ ] 長い1行を入力する
- [ ] 表示メニューで「テキストを折り返す」のチェックを外す（折り返しOFF）
- [ ] 1行のまま表示される
- [ ] 横スクロールバーが表示される
- [ ] 「テキストを折り返す」を再度チェック（折り返しON）
- [ ] 再び折り返される

### dirty

- [ ] 保存済みNoteNestを開き、折り返しだけ切り替える
- [ ] タブが未保存表示にならない

### 再起動

- [ ] 折り返しOFFに変更する
- [ ] NestSuiteを終了・再起動する
- [ ] 折り返しOFFが維持されている

### 複数タブ・別ウィンドウ

- [ ] 複数のNoteNestタブを開いた状態で折り返し設定を変更する
- [ ] すべてのタブへ反映される
- [ ] NoteNestタブを別ウィンドウ化する
- [ ] 折り返し設定が一致している
- [ ] 別ウィンドウ側またはShell側で設定を変更すると、もう一方にも反映される

### 他Workspace

- [ ] IdeaNest / ChatNest / TempNest / PlainTextWorkspace選択中は「テキストを折り返す」の操作が表示へ影響しない

### テーマ・フォント

- [ ] ライトテーマ・ダークテーマの両方で折り返しON/OFFが利用できる
- [ ] 複数のフォント・フォントサイズで折り返しON/OFFが利用できる

---

## NoteNest右ペイン：マーカー／タスクの一括コピー（v2.19.4 M15）

### マーカー

- [ ] 複数ノートにTODO／NOTE（必要ならFIXMEも）マーカーを用意する
- [ ] 右ペインの「マーカー」タブを開く
- [ ] マーカーヘッダーの「全件コピー」を実行する
- [ ] メモ帳等へ貼り付ける
- [ ] 表示順どおりのMarkdown箇条書きになっている
- [ ] コピー完了通知（件数付き）が表示される

### タスク（現行タスク一覧が存在する場合）

- [ ] 未完了・完了タスクを用意する
- [ ] タスクヘッダーの「全件コピー」を実行する
- [ ] メモ帳等へ貼り付ける
- [ ] 完了状態がMarkdownチェックリスト（`- [ ]` / `- [x]`）へ反映される

### 空一覧

- [ ] マーカーが0件（またはフィルタで全種別を外した状態）のとき、「全件コピー」が無効化される
- [ ] タスクが0件のとき、タスクヘッダー自体が表示されない

### dirty

- [ ] 保存済みNoteNestを開き、マーカー・タスクの一括コピーだけを行う
- [ ] タブが未保存表示にならない

### キーボード・アクセシビリティ

- [ ] Tabで「全件コピー」ボタンへ移動できる
- [ ] Enterでコピーが実行される
- [ ] ボタンにToolTip・AutomationProperties.Nameが設定されている

### テーマ

- [ ] ライトテーマ・ダークテーマの両方でボタンの視認性・フォーカス表示に問題がない

---

## IdeaNest：検索語のカード内ハイライト（v2.19.5 ID-7）

### 基本

- [ ] 複数カードを作成し、タイトルと本文の両方に共通する検索語を含める
- [ ] 検索欄へ入力すると一致カードだけが表示される
- [ ] タイトル・本文プレビューの一致箇所が背景色で強調される
- [ ] 検索語を削除すると、カードを再保存・Workspace再読込せずに通常表示へ戻る

### 複数一致

- [ ] 同一カード内のタイトルまたは本文プレビューに検索語を複数回含める
- [ ] 表示範囲内の一致箇所がすべて強調される

### タグだけ一致

- [ ] 検索語をタグだけに含める
- [ ] カードは検索結果に表示される
- [ ] タイトル・本文には不自然なハイライトが出ない

### 大文字・小文字

- [ ] `test` / `TEST` / `Test` を含むカードで、検索結果とハイライト結果が一致する

### カードサイズ・レイアウト

- [ ] カードサイズ S / M / L でタイトル・本文プレビューのレイアウトが崩れない

### テーマ・カード色

- [ ] ライトテーマでハイライト部分の文字が読める
- [ ] ダークテーマでハイライト部分の文字が読める
- [ ] 複数のカード背景色（黄・ピンク・青・緑・紫・オレンジ・グレー・白）でハイライト部分の文字が読める
- [ ] アーカイブ表示中のカードでも文字が読める

### 操作回帰

- [ ] Tabでカードへフォーカス移動できる
- [ ] フォーカス中のカードでEnterを押すとプレビューが開く
- [ ] マウスクリック・右クリックメニューが動作する
- [ ] ピン留め・アーカイブのボタンが動作する

### dirty

- [ ] 保存済みIdeaNestを開き、検索とハイライトだけを行う
- [ ] タブが未保存表示にならない

---

## NoteNest右ペイン内の絞り込み（v2.19.6 L10）

### マーカー

- [ ] 複数ノートにTODO／NOTEマーカーを用意する
- [ ] 右ペインを開く
- [ ] 絞り込み欄へマーカー本文の一部を入力する
- [ ] 一致マーカーだけが表示される
- [ ] ノート名を入力する
- [ ] 所属ノート名一致でも表示される
- [ ] 絞り込みをクリアする
- [ ] 全一覧へ戻る

### 種別フィルタとの併用

- [ ] TODOのみONにする
- [ ] NOTEをOFFにする
- [ ] 文字列絞り込みを入力する
- [ ] 両条件（TODOかつ文字列一致）を満たす項目だけが表示される

### タスク（互換タスクが存在する場合）

- [ ] 複数タスクを用意する
- [ ] タスクタイトルで絞り込む
- [ ] 一致タスクだけが表示される
- [ ] 未完了・完了双方の表示が崩れない

### M15一括コピーとの連携

- [ ] マーカーを絞り込んでから全件コピーする
- [ ] メモ帳等へ貼り付け、表示中の項目だけがコピーされていることを確認する
- [ ] 通知件数と実際の行数が一致する
- [ ] タスクも同様に確認する

### 空状態

- [ ] 一致しない文字列を入力する
- [ ] マーカーの空状態が表示される
- [ ] マーカーの全件コピーボタンが無効になる

### キーボード・アクセシビリティ

- [ ] Tabで絞り込み欄へ移動できる
- [ ] 文字入力できる
- [ ] Tabでクリア操作へ移動できる
- [ ] Enterまたはスペースでクリアできる
- [ ] 絞り込み欄にフォーカスがある状態でEscを押すと絞り込みが解除される

### dirty

- [ ] 保存済みNoteNestを開き、絞り込み・解除・コピーだけを行う
- [ ] タブが未保存表示にならない

### 複数タブ・別ウィンドウ

- [ ] タブAで「予算」、タブBで「契約」のように別々の絞り込み文字列を入力する
- [ ] 各タブで絞り込み状態が独立している
- [ ] 絞り込み中のNoteNestタブを別ウィンドウ化する
- [ ] 絞り込み状態と表示結果が維持される

### テーマ

- [ ] ライトテーマ・ダークテーマの両方で入力欄・クリアボタン・空状態の視認性に問題がない

---

## Shell横断検索：Escapeクローズ・フォーカス復帰（v2.19.9 SH-44）

### NoteNest本文

- [ ] NoteNest本文へカーソルを置く
- [ ] 横断検索を開く（Ctrl+Shift+F）
- [ ] Escapeで閉じる
- [ ] 本文へフォーカスが戻る
- [ ] 続けて文字入力できる

### IdeaNestカード

- [ ] IdeaNestカードへTabでフォーカスする
- [ ] 横断検索を開く
- [ ] Escapeで閉じる
- [ ] 元のカードへフォーカスが戻る
- [ ] Enterでプレビューを開ける

### ChatNest入力欄

- [ ] ChatNest入力欄へフォーカスする
- [ ] 横断検索を開く
- [ ] Escapeで閉じる
- [ ] 入力欄へ戻る
- [ ] 文字入力できる

### タブ見出し

- [ ] タブ見出しへフォーカスする
- [ ] 横断検索を開く
- [ ] Escapeで閉じる
- [ ] タブ見出しへ戻る

### ×ボタン

- [ ] 横断検索を開く
- [ ] ×ボタンで閉じる
- [ ] Escapeと同じフォーカス復帰になる

### 検索結果実行

- [ ] 横断検索を開く
- [ ] 検索結果をEnterまたはクリックで実行する
- [ ] 対象Workspace・対象箇所へ移動する
- [ ] 開く前の古い要素へフォーカスが戻らない

### 復帰先消滅

- [ ] 横断検索を開く
- [ ] 検索中にタブ切替や対象タブを閉じる
- [ ] 横断検索を閉じる
- [ ] 例外が発生しない
- [ ] 現在のWorkspaceまたはタブへ安全にフォーカスされる

### 連打・二重クローズ

- [ ] Escapeを連打しても例外や不自然なフォーカス移動がない
- [ ] ×を連続操作しても例外や不自然なフォーカス移動がない
- [ ] 閉じた直後に再度開いても正しく動作する

### IME

- [ ] 日本語IME変換中に検索欄でEscapeを押したときの挙動を確認する

### テーマ

- [ ] ライトテーマ・ダークテーマの両方でフォーカス復帰後の点線枠・カーソル・カード枠が確認できる

---

## NoteNestマーカー一覧：ListBox化・キーボード選択（v2.19.10 L26）

### Tab到達・上下キー選択

- [ ] NoteNestでマーカーが2件以上あるノートを開く
- [ ] Tabキーでマーカー一覧までフォーカスを移動する
- [ ] 上下キーで一覧内のマーカーが選択移動する
- [ ] 選択移動のみではジャンプしない（本文・ノートが変わらない）

### Enterジャンプ

- [ ] マーカーを選択した状態でEnterを押す
- [ ] 対象ノートが開く（別ノートの場合は切り替わる）
- [ ] 該当行へ移動する
- [ ] 本文エディタへフォーカスが移り、続けて文字入力できる

### マウス操作との一致

- [ ] マウスクリックでのジャンプが従来通り動作する
- [ ] マウスクリックとEnterジャンプで遷移先（ノート・行位置）が一致する

### 種別フィルタ・L10絞り込みとの連携

- [ ] TODO/FIXME/NOTEのいずれかのフィルタを外し、上下キーの選択範囲が表示中の項目だけになる
- [ ] L10の絞り込み文字列を入力し、上下キーの選択範囲が絞り込み後の表示項目だけになる
- [ ] 並び順（抽出順/種別順/ノート順/行番号順）を変更すると、上下キーの移動順も追従する

### 0件時の空状態

- [ ] フィルタ・絞り込みでマーカーが0件になる
- [ ] 空状態の案内文が表示される
- [ ] Tabキーでマーカー一覧にフォーカス矩形が残らない
- [ ] Enterを押しても例外が発生しない

### M15全件コピーとの連携

- [ ] マーカーを1件選択した状態で「全件コピー」を押す
- [ ] 選択有無に関わらず表示中のマーカーが全件コピーされる
- [ ] 絞り込み中でも絞り込み後の表示項目が全件コピーされる

### 選択項目の消滅

- [ ] マーカーを選択した状態でフィルタ・絞り込みを変更し、選択中の項目が表示から消える
- [ ] 例外が発生しない
- [ ] 無関係な項目へ自動ジャンプしない

### テーマ・別ウィンドウ

- [ ] ライトテーマ・ダークテーマの両方で選択中マーカーの視認性に問題がない
- [ ] NoteNestを別ウィンドウへ分離した状態でも同じ操作が成立する

---

## NoteNestリンク一覧：ListBox化・Enterジャンプ（v2.19.11 L27）

### アウトバウンドリンク（からのリンク）

- [ ] 複数の有効なノートリンクを含むノートを選択する
- [ ] 右ペインのリンクタブを開く
- [ ] Tabで「からのリンク」一覧へ移動する
- [ ] 上下キーで項目を選択する
- [ ] 選択だけではノートが切り替わらない
- [ ] Enterで選択リンク先へ移動する
- [ ] マウスクリックと同じ移動先になる

### バックリンク（へのリンク）

- [ ] 複数ノートから参照されているノートを選択する
- [ ] Tabで「へのリンク」一覧へ移動する
- [ ] 上下キーで参照元を選択する
- [ ] 選択だけではノートが切り替わらない
- [ ] Enterで参照元ノートへ移動する
- [ ] マウスクリックと同じ移動先になる

### リンク切れ

- [ ] 存在しないノート名へのリンクを用意する
- [ ] リンク切れ項目を選択する
- [ ] ⚠表示とToolTip「リンク切れ」を確認する
- [ ] Enterを押す
- [ ] 例外が出ない
- [ ] 別ノートへ誤移動しない
- [ ] 未保存表示にならない

### 0件・ノート未選択

- [ ] アウトバウンドリンク0件のとき「（なし）」が表示され、空一覧へ不自然にTab移動しない
- [ ] バックリンク0件のとき「（なし）」が表示され、空一覧へ不自然にTab移動しない
- [ ] ノート未選択のとき「ノートを選択してください」が表示され、Enterで例外が出ない

### 選択項目の消滅

- [ ] リンク項目を選択した状態で対象ノートを切り替え、旧選択が残らないことを確認する
- [ ] リンク項目を選択した状態で本文からそのリンクを削除し、一覧更新後に例外が出ず旧項目へEnter移動しないことを確認する

### スクロール

- [ ] アウトバウンドリンク・バックリンクを多数用意し、上下キーで画面外項目へ移動したときに選択項目が表示範囲へ追従する
- [ ] スクロールバーが不自然に二重表示されない
- [ ] マウスホイールが見出しを含め両一覧で自然に動く

### テーマ・別ウィンドウ

- [ ] ライトテーマ・ダークテーマの両方で選択中項目の視認性に問題がない
- [ ] NoteNestを別ウィンドウへ分離した状態でも同じ操作が成立する

---

## NoteNestタスクグループ開閉・タスクコメント表示：キーボード対応（v2.19.12 L28）

### グループ開閉

- [ ] NoteNest右ペインのタスクタブを開く
- [ ] Tabでグループ見出しへ移動する
- [ ] Enterで折りたたむ
- [ ] 同じ見出しへフォーカスが残る
- [ ] Spaceで展開する
- [ ] マウスクリックでも同じ状態になる

### 複数グループ

- [ ] 複数のタスクグループを用意する
- [ ] Tabで各見出しへ順番に移動する
- [ ] Enter／Spaceで個別に開閉する
- [ ] 別グループへ誤作用しない

### タスクタイトル

- [ ] Tabでタスクタイトルへ移動する
- [ ] Enterでコメント表示を切り替える
- [ ] 同じタイトルへフォーカスが残る
- [ ] もう一度Enterを押しても例外や不自然な挙動が起きない
- [ ] マウスの既存操作（ダブルクリック）でも同じ状態になる

### CheckBox

- [ ] タスクCheckBoxへTab移動する
- [ ] Spaceで完了／未完了を切り替える
- [ ] 親グループが開閉しない
- [ ] コメント表示が切り替わらない

### 折りたたみ時のフォーカス安全性

- [ ] グループ内のタスクタイトルへフォーカスする
- [ ] 見出しへ戻る
- [ ] グループを折りたたむ
- [ ] フォーカスが見えない子要素へ残らない

### dirty

- [ ] 保存済みNoteNestでグループ開閉・コメント表示切替を行っても未保存表示にならない
- [ ] CheckBox操作による既存dirty契約が従来どおりである

### フォーカス視認性・レイアウト

- [ ] ライトテーマ・ダークテーマの両方でグループ見出し・タスクタイトルのフォーカスが視認できる
- [ ] 狭いウィンドウ幅・高DPI・長いタスクタイトル・長いコメントでもレイアウト崩れがない

### 別ウィンドウ

- [ ] NoteNestを別ウィンドウへ分離した状態でもTab到達・グループ開閉・コメント表示切替・CheckBox操作・フォーカス維持が成立する

---

## ChatNestメッセージコンテナ：フォーカス可能化・ContextMenuキーボード導線（v2.19.13 CH-19）

### Tab到達

- [ ] 複数メッセージを用意してChatNestを開く
- [ ] Tabで過去メッセージへ順番に移動する
- [ ] フォーカス中メッセージが視認できる

### Shift+F10

- [ ] 任意のメッセージへフォーカスする
- [ ] Shift+F10を押す
- [ ] そのメッセージのContextMenuが開く
- [ ] 上下キーで項目を選択できる
- [ ] Escapeで閉じる
- [ ] 元のメッセージへフォーカスが戻る

### コンテキストメニューキー

- [ ] メッセージへフォーカスした状態でコンテキストメニューキーを押す
- [ ] Shift+F10と同じContextMenuが開く

### 操作対象の一致

- [ ] メッセージAへフォーカス→Shift+F10→本文コピー→Aの本文がコピーされる
- [ ] メッセージBへフォーカス→Shift+F10→編集→Bが編集対象になる
- [ ] 以前操作したメッセージが誤って対象にならない

### マウス右クリック

- [ ] マウス右クリックで従来どおり同じContextMenuが開く
- [ ] 右クリックしたメッセージが操作対象になる

### 編集・削除・コピー

- [ ] Shift+F10→編集→既存編集モードへ移行し、編集欄へフォーカスする
- [ ] 既存の確定・キャンセル操作が可能
- [ ] Shift+F10→削除→既存確認表示が出て、キーボードでキャンセル・削除確定ができる
- [ ] 削除後にフォーカスが不可視要素へ残らない
- [ ] Shift+F10→本文コピー→正しい本文がコピーされ、未保存表示にならない

### 投稿欄・会話内検索・編集中のキー入力（非干渉確認）

- [ ] 入力欄でEnter改行・Ctrl+Enter投稿・Ctrl+左右の発言者切替が維持されている
- [ ] Ctrl+Fで検索欄を開き、Enter/Shift+Enter・Escapeが維持されている
- [ ] 編集TextBox内での文字入力・既存編集操作中に親メッセージのContextMenu処理が誤作動しない

### 多数メッセージ・スクロール

- [ ] 十分な件数のメッセージでTabにより画面外メッセージへ移動する
- [ ] フォーカス対象が表示範囲へ追従し、スクロールが不自然に飛ばない

### ライト・ダーク

- [ ] 通常・マウスホバー・キーボードフォーカス・編集モード・削除確認・ContextMenuの各状態を両テーマで確認する

### 別ウィンドウ

- [ ] ChatNestを別ウィンドウへ分離した状態でもTab到達・Shift+F10・ContextMenu操作・編集・削除・コピー・フォーカス復帰が成立する

### dirty

- [ ] 保存済みChatNestでメッセージへTab→Shift+F10→Escapeを行っても未保存表示にならない
- [ ] 既存の編集・削除・並び替えでは従来どおり未保存表示になる

---

## アイコン・記号のみボタンのAutomationName補完（v2.19.14 SH-45）

### キーボード操作

- [ ] Tabで対象ボタン（タブ閉じる・横断検索閉じる・新規タブ追加・タブ一覧・コメント編集・保存・右ペイン開閉・タグパネル切替・検索クリア・再シャッフル・ランダムプレビュー・カードサイズS/M/L・ピン留め・アーカイブ・削除・ChatNest検索バー各ボタン）へ移動できる
- [ ] Enter／Spaceで従来どおり実行できる
- [ ] 今回の変更でTab順が変わっていない

### マウス操作

- [ ] クリックで従来どおり動作する
- [ ] ToolTipが従来どおり表示される
- [ ] 表示・サイズ・余白が変わっていない

### UIA／スクリーンリーダー確認

利用可能な範囲でWindows NarratorまたはInspect.exe等のUIA確認ツールを使用する。

- [ ] タブ閉じるボタンが「×」ではなく「このタブを閉じる」と認識される
- [ ] 横断検索閉じるボタンが「横断検索を閉じる」と認識される
- [ ] 新規タブ追加ボタンが「新規タブを追加」、タブ一覧ボタンが「タブ一覧を開く」と認識される（内部ID文字列が読み上げられない）
- [ ] NoteNest右ペイン開閉ボタンが、開いている/閉じている状態に応じて「右ペインを閉じる」／「右ペインを表示」と認識される
- [ ] IdeaNestのピン留め・アーカイブ切替ボタンが、ピン留め済み/未ピン留め、アーカイブ済み/未アーカイブの状態に応じた名前で認識される
- [ ] 利用できない場合は、静的確認のみであることを記録する

### 状態変化

- [ ] NoteNest右ペインを開く→閉じるでAutomationNameが切り替わる
- [ ] IdeaNestカードをピン留め→解除でAutomationNameが切り替わる
- [ ] IdeaNestカードをアーカイブ→復元でAutomationNameが切り替わる

### テーマ・レイアウト・別ウィンドウ

- [ ] ライトテーマ・ダークテーマで表示が変わっていない
- [ ] 狭いウィンドウ幅・高DPIで表示が変わっていない
- [ ] 別ウィンドウ化した状態でも対象ボタンの動作・AutomationNameが同様に機能する

### dirty

- [ ] 保存済みWorkspaceで対象ボタンへTab移動・ToolTip表示・UIA確認を行っても未保存表示にならない
- [ ] 各ボタンの既存操作（保存・削除・ピン留め等）を実行した場合は、その操作の従来のdirty契約が維持されている

---

## Shellメニュー・タブContextMenuのアクセスキー重複解消（v2.19.15 SH-46）

### ファイルメニュー

- [ ] Alt+Fでファイルメニューを開く
- [ ] Nで「新規作成」が1回のキー入力で開ける
- [ ] Escapeでメニューを閉じる
- [ ] Alt+F→Vで「名前を付けて保存」が1回のキー入力で選べる（NとVで循環しない）
- [ ] 名前を付けて保存で標準保存ダイアログが従来どおり開く

### 新規作成の子メニュー

- [ ] Alt+F→N→N/I/C/Tで、それぞれNoteNest/IdeaNest/ChatNest/テキストの新規作成が従来どおり実行できる

### タブContextMenu

- [ ] 分離可能なタブへフォーカスし、Shift+F10でContextMenuを開く
- [ ] Bで「このタブへ戻す」が実行でき、対象タブがShellへ戻る
- [ ] 右側に複数タブがある状態で、Rで「右側のタブを閉じる」が実行でき、右側の対象タブだけが閉じる
- [ ] 「このタブへ戻す」と「右側のタブを閉じる」が循環しない

### 表示条件

- [ ] 通常タブ・ピン留めタブ・分離中タブ・Tempタブ・右側にタブがない状態それぞれで、VisibilityやIsEnabledが従来どおりである

### 保存ショートカット・マウス操作

- [ ] Ctrl+S・Ctrl+Shift+Sが従来どおり動作する
- [ ] 各MenuItemをマウスクリックして従来どおり同じ処理が実行される

### dirty

- [ ] Alt+Fでメニューを開いて移動し、Escapeで閉じても未保存表示にならない
- [ ] 名前を付けて保存を実行した場合は従来の保存状態更新が維持される

### ライト・ダーク

- [ ] アクセスキー下線・MenuItem表示・無効状態・ToolTipがライト・ダークテーマ双方で従来どおり表示される

---

## ChatNest発言 → IdeaNestカード化（v2.21.0 LK-4）

### IdeaNestタブ0件

- [ ] IdeaNestタブを開いていない状態でChatNest発言を右クリック→「IdeaNestカードに追加」を実行する
- [ ] 案内の一時通知のみが表示され、新規IdeaNestタブが自動生成されない
- [ ] ChatNestの表示・内容が変化せず、dirty表示にもならない

### IdeaNestタブ1件

- [ ] IdeaNestタブを1つ開いた状態で発言をカード化する
- [ ] 選択ダイアログが表示されず、そのタブへ直接カードが追加される
- [ ] 追加されたカードの本文が発言本文と一致し、タイトルはIdeaNest既存の生成規則（本文先頭行）で決まる
- [ ] IdeaNestタブに未保存（dirty）表示が付く
- [ ] ChatNestの表示位置・内容・並び順が変化せず、選択中タブがChatNestのままである
- [ ] 一時通知でカード追加が伝わる

### IdeaNestタブ複数件

- [ ] IdeaNestタブを2つ以上開いた状態で発言をカード化する
- [ ] 対象選択ダイアログが表示され、上下キーで選択・Enterで確定・Escapeでキャンセルできる
- [ ] 選択したタブだけにカードが追加され、他のIdeaNestタブは変化しない
- [ ] 同名タブが複数ある場合でも、選んだタブへ正しく追加される
- [ ] 選択ダイアログを開いた時点でListBoxへ初期フォーカスがある
- [ ] マウスダブルクリックでも決定できる

### 選択ダイアログのAutomationName（v2.21.1 LK-4-1）

- [ ] Narrator等のスクリーンリーダーを利用できる場合、転送先一覧・OK・キャンセルの読み上げ名が
      `Dialog.WorkspaceTransferTargetList` / `Dialog.OkButton` / `Dialog.CancelButton` のような
      内部IDのままではなく、意味の分かる日本語（例: 「転送先のIdeaNestタブ一覧」「転送先を決定」「キャンセル」）
      で読み上げられることを確認する
- [ ] 利用できない場合は未確認として明記する

### 別ウィンドウ表示中のIdeaNest

- [ ] 別ウィンドウ化したIdeaNestタブも選択候補に表示される（表示名に「（別ウィンドウ）」）
- [ ] 選択後にカードが追加され、別ウィンドウが自動的に前面化しない

### キーボード操作

- [ ] Tabで対象ChatNestメッセージへフォーカスし、Shift+F10でContextMenuを開き、Iで「IdeaNestカードに追加」を実行できる
- [ ] コンテキストメニューキーからも同様に実行できる
- [ ] マウス右クリックでも同じContextMenuから実行できる

### 失敗時

- [ ] 可能な範囲で失敗系（対象タブなし等）を確認し、不要なErrorLog記録が発生しないことを確認する
- [ ] 予期しない例外が発生した場合のみエラーダイアログが表示される

### 保存・再起動

- [ ] 転送後、IdeaNestタブでCtrl+Sを実行すると通常どおり保存される
- [ ] 保存したIdeaNestファイルを再起動後に開き、追加したカードが正しく保持されている

### dirty・非対象

- [ ] 転送元ChatNestがいかなる場合もdirtyにならない
- [ ] TN-3（TempNest→NoteNest昇格）の動作が従来どおりである

---

## TempNestスロット → IdeaNestカード追加（v2.22.0 LK-3）

### IdeaNestタブ0件

- [ ] IdeaNestタブを開いていない状態でTempNestスロットにTitle/Bodyを入力し「IdeaNestカードに追加」を実行する
- [ ] 案内の一時通知のみが表示され、新規IdeaNestタブが自動生成されない
- [ ] TempNestの表示・内容が変化しない

### IdeaNestタブ1件

- [ ] IdeaNestタブを1つ開いた状態でスロットをカード化する
- [ ] 選択UIが表示されず、そのタブへ直接カードが追加される
- [ ] 追加されたカードのTitle・BodyがTempNestスロットのTitle・Bodyと一致する
- [ ] IdeaNestタブに未保存（dirty）表示が付く
- [ ] TempNestに留まり、IdeaNestへ自動切替されない
- [ ] 一時通知でカード追加が伝わる
- [ ] 追加後、元スロットを「消去する」「残す」の両方を確認する（既定は「残す」）

### IdeaNestタブ複数件

- [ ] IdeaNestタブを2つ以上開いた状態でスロットをカード化する
- [ ] 既存の`WorkspaceTransferTargetDialog`が表示され、上下キーで選択・Enterで確定・Escapeでキャンセルできる
- [ ] 選択したタブだけにカードが追加され、他のIdeaNestタブは変化しない
- [ ] 同名タブが複数ある場合でも、選んだタブへ正しく追加される
- [ ] キャンセルした場合、TempNest・IdeaNestとも変化しない

### 別ウィンドウ表示中のIdeaNest（Detached）

- [ ] 別ウィンドウ化したIdeaNestタブも選択候補に表示される
- [ ] 選択後にカードが追加され、別ウィンドウが自動的に前面化しない

### タイトルのみ・本文空

- [ ] Titleのみ入力・Body空の状態で「IdeaNestカードに追加」を実行すると、Titleのみのカードが追加される
- [ ] Title・Bodyとも空の場合はボタンが無効化されている

### キーボード操作

- [ ] Tabで対象スロットの「IdeaNestカードに追加」ボタンへフォーカスし、Enter/Spaceで実行できる
- [ ] マウスクリックでも同じ処理が実行できる

### 失敗時

- [ ] 可能な範囲で失敗系（対象タブなし・キャンセル等）を確認し、不要なErrorLog記録が発生しないことを確認する
- [ ] 予期しない例外が発生した場合のみエラーダイアログが表示される
- [ ] 失敗・キャンセル時はTempNestの元スロットが一切変更されない

### 保存・再起動

- [ ] 転送後、IdeaNestタブでCtrl+Sを実行すると通常どおり保存される
- [ ] 保存したIdeaNestファイルを再起動後に開き、追加したカードが正しく保持されている

### dirty・非対象

- [ ] 転送先IdeaNest以外（TempNest自身）は転送成功後処理までdirtyにならない
- [ ] TN-3（TempNest→NoteNest昇格）の動作が従来どおりである
- [ ] LK-4（ChatNest→IdeaNest）の動作が従来どおりである
- [ ] LK-2（TempNest→NoteNest既存タブ）は v2.23.0 で実装済み。手動確認項目は下記「TempNestスロット → 既存NoteNestタブへノート追加（v2.23.0 LK-2）」参照

---

## TempNestスロット → 既存NoteNestタブへノート追加（v2.23.0 LK-2）

### NoteNestタブ0件

- [ ] NoteNestタブを開いていない状態でTempNestスロットにTitle/Bodyを入力し「既存NoteNestへ追加」を実行する
- [ ] 案内の一時通知（「NoteNest タブがありません。NoteNest を開いてから実行してください」）のみが表示され、新規NoteNestタブが自動生成されない
- [ ] TempNestの表示・内容が変化しない
- [ ] ErrorLogに記録が残らない

### NoteNestタブ1件

- [ ] NoteNestタブを1つ開いた状態でスロットを既存タブへ追加する
- [ ] 選択UIが表示されず、そのタブへ直接新規ノートが追加される
- [ ] 追加されたノートのTitleがTempNestスロットのTitleと一致する（Title未入力時はTN-3と同じフォールバックタイトルが生成される）
- [ ] 追加されたノートのContentがTempNestスロットのBodyと一致する
- [ ] NoteNestタブに未保存（dirty）表示が付く
- [ ] TempNestに留まり、NoteNestへ自動切替されない
- [ ] 一時通知（「NoteNest「〈タブ名〉」にノートを追加しました」）でノート追加が伝わる
- [ ] 追加後、元スロットを「消去する」「残す」の両方を確認する（既定は「残す」）

### NoteNestタブ複数件

- [ ] NoteNestタブを2つ以上開いた状態でスロットを既存タブへ追加する
- [ ] 既存の`WorkspaceTransferTargetDialog`が表示され、上下キーで選択・Enterで確定・Escapeでキャンセルできる（プロンプト文言は「転送先のNoteNestタブを選択してください:」）
- [ ] 選択したタブだけにノートが追加され、他のNoteNestタブは変化しない
- [ ] 同名タブが複数ある場合でも、選んだタブへ正しく追加される（TabIdで識別されていることの確認）
- [ ] キャンセルした場合、TempNest・NoteNestとも変化しない

### 別ウィンドウ表示中のNoteNest（Detached）

- [ ] 別ウィンドウ化したNoteNestタブも選択候補に表示される
- [ ] 選択後にノートが追加され、別ウィンドウが自動的に前面化しない

### タイトルなし・タイトルあり・重複タイトル

- [ ] Body只のみ入力・Title空の状態で「既存NoteNestへ追加」を実行すると、TN-3と同じ既存タイトル生成規則でタイトルが付く
- [ ] Titleを入力した状態で実行すると、入力したTitleがそのままノートタイトルになる
- [ ] 追加先NoteNestタブに同名のノートが既に存在する場合、末尾に連番（例:「タイトル (2)」）が付いて追加される（失敗しない）
- [ ] Title・Bodyとも空の場合はボタンが無効化されている

### キーボード操作

- [ ] Tabで対象スロットの「既存NoteNestへ追加」ボタンへフォーカスし、Enter/Spaceで実行できる
- [ ] マウスクリックでも同じ処理が実行できる
- [ ] ボタンのAutomationNameが内部AutomationId文字列ではなく、可視テキスト「既存NoteNestへ追加」から自然に導出されている（Narrator等で確認）
- [ ] ボタンにToolTip「スロットのタイトル・本文を既存のNoteNestタブへ新規ノートとして追加します」が表示される

### 失敗時

- [ ] 可能な範囲で失敗系（対象タブなし・キャンセル等）を確認し、不要なErrorLog記録が発生しないことを確認する
- [ ] 予期しない例外が発生した場合のみエラーダイアログが表示される
- [ ] 失敗・キャンセル時はTempNestの元スロットが一切変更されない

### 保存・再起動

- [ ] 転送後、NoteNestタブでCtrl+Sを実行すると通常どおり保存される
- [ ] 保存したNoteNestファイルを再起動後に開き、追加したノートが正しく保持されている

### dirty・非対象・回帰

- [ ] 転送先NoteNest以外（TempNest自身）は転送成功後処理までdirtyにならない
- [ ] TN-3（TempNest→新規NoteNestタブへ昇格）の動作が従来どおりである（本文のみのフォールバックタイトル生成含む）
- [ ] TN-3のボタン表示が「新規NoteNestへ昇格」に変わっているが、Command・挙動は従来どおりである
- [ ] LK-3（TempNest→IdeaNest）の動作が従来どおりである
- [ ] LK-4（ChatNest→IdeaNest）の動作が従来どおりである

---

## チェックリスト追加基準

チェック項目を追加・変更する際は以下の基準に従う。

- **常用チェックリストに置くもの**: 毎回のリリースで確認すべき機能・品質確認
- **補足文書（history）へ置くもの**: 一時的な注意、特定バージョンの経緯、背景説明、既知の非ブロッキング警告
- 過去の失敗履歴をそのまま常用項目化しない
- 追加した項目が長文化した場合は、確認文と背景説明を分ける
- 「念のため」の項目を増やしすぎない
- 重要な安全確認が埋もれないことを優先する
