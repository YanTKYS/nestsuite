# migrations archive

このフォルダーは、完了済みの移行計画、名称変更、起動経路変更などの履歴保管場所です。

現在の運用手順ではありません。現在の配布・運用では `docs/operations/` 配下の Canonical 文書を優先してください。

過去の移行手順を現在の環境へそのまま適用しないでください。

移行・名称変更履歴には、`.nestsuite` 拡張子統一の完了記録（`workspace-file-extension-unification.md`）も含みます。
