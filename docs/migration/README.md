# docs/migration — ディレクトリ概要

このディレクトリには、現行の個別 migration 文書は残っていません。

v1.11.0〜v1.19.x 時代の完了済み移行計画は、v2.17.5 / TD-80 で `docs/archive/migrations/` へ移設しました。

---

## 移設済み文書

| 旧文書 | 移設先 | 状態 |
|------|------|------|
| `nestsuite-default-startup-plan.md` | `docs/archive/migrations/nestsuite-default-startup-plan.md` | `--classic-notenest` は v1.19.3 で削除済み。移行完了 |

現行開発で参照すべき文書はこのディレクトリに含まれません。
現在の配布・運用手順は `docs/operations/` 配下の Canonical 文書を優先してください。

この README 自体は、上表文書の `[履歴文書]` 冒頭注記が「詳細はこちら」として直接リンクする移設先索引です。
v2.19.7 / TD-83 で削除可否を再判定し、参照元文書のリンク先として現役の役割があるため維持と判断しました（`docs/planning/docs-inventory-and-archive-policy.md` 参照）。
